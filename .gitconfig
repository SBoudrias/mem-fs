[hook "nano-staged"]
	event = pre-commit
	command = sh -c 'yarn nano-staged'

[hook "install-after-checkout"]
	event = post-checkout
	command = sh -c 'yarn install'

[hook "verify-before-push"]
	event = pre-push
	command = sh -c 'CI=true yarn pretest && yarn test'
